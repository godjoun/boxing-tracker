# 도장 화면 구조 (참고 분석 → 우리 반영)

> UI 복제 금지. **흐름·자리만** 빌린다.  
> 제품: `docs/dojo.md` · 수익: `docs/monetization.md`

## 참고 앱에서 가져온 것

| 앱 | 빌린 구조 | 안 가져온 것 |
|----|-----------|--------------|
| **당근** 비즈프로필 | 찾기 → **가게 홈**(커버·소개·가격) → 문의·채팅 | 단골·쿠폰·후기 피드 |
| **플랩** | 장소/매치 카드 → **상세** → 신청(행동 1개) | 소셜 매치·레벨·구장 예약 풀스택 |
| **Boxing Network** | 짐/이벤트 **허브** + 문의·매칭은 다음 행동 | 팀 가입·bout sheet·코치 워크플로 |
| **더코너류** | (나중) **관에 붙는** 소식·멤버 | 지금 전역 커뮤니티 피드 |

## 체육관 문의·대여 — 역할 3탭 (잠금)

| 탭 | 누구 | 흐름 |
|----|------|------|
| **찾기** | 복서 | 검색 → 관 상세 → 문의하기 → 대화 |
| **내 문의** | 복서 | 보낸 문의 → 미리보기·안 읽음 → 대화하기 |
| **내 관** | 관장 | 등록·관리 · 받은 문의 → 미리보기·안 읽음 → 대화하기 |

탭에 안 읽음 숫자 배지. SQL: `dojo_inquiry_chat.sql` + `dojo_inquiry_chat_inbox.sql`  
푸시·Realtime은 나중.

```
찾기 (기본)
  └ 관 한 장면 → 문의 / (내 관이면) 받은 문의

내 문의
  └ 보낸 목록 → 대화

내 관
  └ 등록 관리 · 받은 문의 → 대화
```

연락처는 공개하지 않는다. 답장은 앱 대화가 기본.

## 코드

- 세그먼트: `NearbyGymsPanel` (`section`: find / sent / owner)
- 목록 카드: `GymResultCard`
- 상세: `GymDetailPanel`
- 보낸/받은: `GymSentInquiriesPanel` · `GymInquiryLedgerPanel`
- 대화: `GymInquiryChatModal`
