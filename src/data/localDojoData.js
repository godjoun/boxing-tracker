/** 예전 예시 체육관 — 검색에서는 쓰지 않음 (입점 approved만) */
export const LOCAL_GYMS = [];
/** 모임 예시 시드 — 노출하지 않음 (실제 일정만) */
export const LOCAL_EXCHANGE_EVENTS = [];
/** 라이벌 예시 시드 — 노출하지 않음 (군산 실제 공개 카드만) */
export const LOCAL_SPARRING_PARTNERS = [];
